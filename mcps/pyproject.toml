[project]
name = "mcps"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp>=1.26.0",
    "numpy>=2.4.3",
    "pydantic>=2.12.5",
]

[tool.pyright]
typeCheckingMode = "strict"

[tool.ruff]
line-length = 88
